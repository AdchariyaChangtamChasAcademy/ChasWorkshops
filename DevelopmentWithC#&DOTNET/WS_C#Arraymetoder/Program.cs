namespace WorkshopC_Arraymetoder
{
    internal class Program
    {
        static void Main(string[] args)
        {

            // Föreläsningsövning 09/09/2025
            /*
            string[] names = { "Sara", "Alex", "Reidar", "Maria"};

            // 1. Sortea alfabetiskt
            Array.Sort(names);
            foreach (string name in names)
            {
                Console.Write(name + " ");
            }
            Console.WriteLine(" ");

            // 2. Sök efter ett visst namn
            Console.Write("Namn att söka: ");
            string nameToSearch = Console.ReadLine();
            int nameToSearchIndex = Array.IndexOf(names, nameToSearch);
            if(nameToSearchIndex >= 0)
            {
                Console.Write($"Namnet {nameToSearch} finns på index: {nameToSearchIndex}");
            }
            else
            {
                Console.Write($"Namnet {nameToSearch} finns inte i listan!");
            }
            Console.WriteLine(" ");

            // 3. Vänd på ordningen.
            Array.Reverse(names);
            foreach (string name in names)
            {
                Console.Write(name+" ");
            }
            Console.WriteLine(" ");
            */


            Console.ReadLine();
        }
    }
}
